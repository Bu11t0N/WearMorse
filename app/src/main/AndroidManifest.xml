<?xml version="1.0" encoding="utf-8"?>
<!-- AndroidManifest.xml модуля :app (телефон) -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Разрешения для Wearable API, фонового сервиса и воспроизведения аудио -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WearMorseMessenger">

        <!-- Главный экран: поле ввода и кнопка «Отправить» -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Фоновый сервис: принимает аудиопоток с часов и воспроизводит его -->
        <service
            android:name=".AudioReceiverService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Обязательная мета-данная для связи с Wear OS -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />
    </application>
</manifest>
